const http = require('http'); const postData = JSON.stringify({username: 'admin', password: 'password'}); const options = {hostname: '100.28.238.125', port: 3330, path: '/api/auth/login', method: 'POST', headers: {'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(postData)}}; const req = http.request(options, (res) => {let data = ''; res.on('data', (chunk) => {data += chunk;}); res.on('end', () => {console.log(data);});}); req.on('error', (e) => {console.error(problem with request: );}); req.write(postData); req.end();